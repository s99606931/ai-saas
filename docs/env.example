# 공공기관 SaaS 플랫폼 — 환경변수 설정
# Design Ref: DESIGN-MTU-DEP2
# Plan SC: FR-DEP2.5
#
# 사용법: cp docs/env.example .env 후 값 수정
# 주의: .env 파일은 절대 커밋 금지 (CSAP D-09)

# ============================================================
# 데이터베이스 (PostgreSQL 16)
# ============================================================
DB_USER=saas
DB_PASSWORD=saas_dev_2026
DB_NAME=saas_platform
DB_PORT=5432

# ============================================================
# Redis 7
# ============================================================
REDIS_PASSWORD=redis_dev_2026
REDIS_PORT=6379

# ============================================================
# MinIO (S3 호환 오브젝트 스토리지)
# ============================================================
MINIO_USER=minio_admin
MINIO_PASSWORD=minio_dev_2026
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# ============================================================
# 인증 (JWT)
# ============================================================
# 운영 환경에서는 반드시 강력한 시크릿으로 변경 필수
JWT_SECRET=dev-jwt-secret-change-in-production

# ============================================================
# AI 서비스 (LM Studio — 온프레미스 LLM)
# ============================================================
# WSL2에서 Windows 호스트의 LM Studio 접근
LM_STUDIO_URL=http://host.docker.internal:1234

# ============================================================
# 일반 설정
# ============================================================
NODE_ENV=development
LOG_LEVEL=info

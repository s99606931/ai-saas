# audit-service ExternalSecret
# Design Ref: MTU-N66.design.md §1
# Plan SC: FR-N66.3
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: audit-service-secrets
  namespace: saas
  labels:
    app.kubernetes.io/part-of: saas-platform
    app.kubernetes.io/component: audit-service
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-backend
    kind: SecretStore
  target:
    name: audit-service-env
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: audit-service-master
        property: DATABASE_URL
    - secretKey: STORAGE_CONFIG
      remoteRef:
        key: audit-service-master
        property: STORAGE_CONFIG

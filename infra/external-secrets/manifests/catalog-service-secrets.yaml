# catalog-service ExternalSecret
# Design Ref: MTU-N66.design.md §1
# Plan SC: FR-N66.3
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: catalog-service-secrets
  namespace: saas
  labels:
    app.kubernetes.io/part-of: saas-platform
    app.kubernetes.io/component: catalog-service
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-backend
    kind: SecretStore
  target:
    name: catalog-service-env
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: catalog-service-master
        property: DATABASE_URL
    - secretKey: CACHE_CONFIG
      remoteRef:
        key: catalog-service-master
        property: CACHE_CONFIG

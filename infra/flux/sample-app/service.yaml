# GitOps 데모 Service
# Design Ref: MTU-N24 §5
apiVersion: v1
kind: Service
metadata:
  name: nginx-demo
  namespace: gitops-demo
  labels:
    app: nginx-demo
    app.kubernetes.io/managed-by: flux
spec:
  selector:
    app: nginx-demo
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
